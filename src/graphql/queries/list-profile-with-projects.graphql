query ListProfileWithProjects($id: ID) {
  profile(where: { id: $id }) {
    id
    name
    email
    birthday
    from
    github
    linkedin
    job
    projects {
      slug
      title
      type
      url
      videoUrl
      description
    }
  }
}
